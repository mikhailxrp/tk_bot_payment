# Статус фаз

Единый трекер прогресса. Детальное описание каждой фазы (Что/Почему/DoD) — в `phases.md`.
Правила обновления:

- Начал фазу → статус 🔄, создан `phase-N.md` (через `.docs/prompts/phase-init.md`)
- Закрыл фазу → статус ✅, дата и номер PR
- Фаза берётся в работу, только если все её зависимости ✅

Легенда: ⏳ Ожидает · 🔄 В работе · ✅ Готово · ⛔ Заблокировано


| N   | Фаза                                        | Зависит от | Статус | Файл фазы                | PR / дата       |
| --- | ------------------------------------------- | ---------- | ------ | ------------------------ | --------------- |
| 1   | Каркас монорепо и инфраструктура            | —          | ✅      | [phase-1.md](phase-1.md) | — / 2026-07-04  |
| 2   | Базовый бот: /start и меню подписки         | 1          | ✅      | [phase-2.md](phase-2.md) | — / 2026-07-04  |
| 3   | Робокасса: платёжная ссылка + webhook       | 2          | ✅      | [phase-3.md](phase-3.md) | все таски ✅ / — |
| 4   | Два продукта: общая группа + выбор в /start | 3          | ✅      | [phase-4.md](phase-4.md) | все таски ✅ / 2026-07-05 |
| 5   | Выдача доступа в группы                     | 4          | ✅     | [phase-5.md](phase-5.md) | 5.1–5.4 ✅, 5.5 автотесты ✅ / — |
| 6   | Ежедневный cron: mute истёкших              | 5          | 🔄      | [phase-6.md](phase-6.md) | —               |
| 7   | Напоминания (оба типа)                      | 6          | ⏳      | —                        | —               |
| 8   | Панель: каркас + auth                       | 1          | ⏳      | —                        | —               |
| 9   | Панель: пользователи, платежи, настройки    | 5, 8       | ⏳      | —                        | —               |
| 10  | Панель: дашборд и аналитика                 | 9          | ⏳      | —                        | —               |
| 11  | Тесты, деплой, приёмка                      | 7, 10      | ⏳      | —                        | —               |


## Примечания

- 2026-07-05 фазы перенумерованы: вставлена новая Фаза 4 «Два продукта», прежние 4–10 стали 5–11
  (см. журнал решений).
- Фазы 8–10 (панель) можно вести параллельно с 6–7 (cron), если работают два агента:
пересечение только в фазе 9 (internal API добавляется в `apps/bot`).
- Таск `services/notify.ts` (спланирован для «Выдачи доступа», теперь Фаза 5) продукто-независим
  и может выполняться параллельно с Фазой 4.
- Блокер фазы 3: доступ к кабинету Робокассы клиента + его система налогообложения
(`sno`/`tax` для Receipt). Не начинать фазу 3 без этого.



## Журнал решений

*(фиксировать здесь решения, меняющие план: дата — решение — что изменилось)*

- 2026-07-03 — Панель на Server Actions (не REST API Routes); mute/unmute из панели через internal API бота.
- 2026-07-04 — Завершена Фаза 1 ([phase-1.md](phase-1.md)): таски 1.1–1.5 ✅; PR `phase-1` → `dev` — ожидает подтверждения.
- 2026-07-04 — Завершена Фаза 2 ([phase-2.md](phase-2.md)): таски 2.1–2.3 ✅ (`/start`, меню подписки,
`isAdmin` + `/admin`); PR `phase-2` → `dev` — ожидает подтверждения.
- 2026-07-04 — `period_days` — источник истины срока подписки; продление (Фаза 3) считать `+ period_days`, не хардкод 30. CLAUDE.md п.9 / prd п.3.2 = «30 как дефолт настройки».
- 2026-07-04 — Seed сидирует только 5 ключей `Setting`; тексты сообщений остаются в коде, пункт «текст сообщений» убран из scope настроек Фазы 8 (теперь Фаза 9).
- 2026-07-04 — `config.ts` (zod) только в `apps/bot`; единый `.env` в корне; zod-валидация env админки перенесена в DoD Фазы 7 (теперь Фаза 8).
- 2026-07-04 — На этапе разработки MySQL и приложения запускаем локально (не в Docker); `docker-compose.yml` — опциональная альтернатива.
- 2026-07-04 — Пересмотрено: локальная MySQL/Docker не используются вообще. На разработке
подключаемся напрямую к реальной, уже созданной MySQL-базе на хостинге через `DATABASE_URL`;
`docker-compose.yml` удалён. Приложения (бот/панель) остаются локальными node-процессами.
- 2026-07-05 — Task 3.5 ✅ ([phase-3.md](phase-3.md)): vitest настроен, 15 автотестов (подписи
Робокассы, Receipt-кодирование, продление, webhook). Фаза 3: все таски готовы; остались
ручная проверка `IsTest=1` и PR `phase-3` → `dev`.
- 2026-07-05 — Решения по «Выдаче доступа» (спланирована как Фаза 4, после перенумерации — Фаза 5):
`allowed_updates` обязателен для `chat_member` (иначе join-хендлер не сработает); Telegram-вызовы
после оплаты (invite-ссылка, уведомления) выполняются после коммита транзакции, best-effort —
сбой логируется и уходит алертом админам, без auto-retry; в результат транзакции webhook
добавляется флаг `freshlyProcessed`, чтобы повторный (идемпотентный) POST не дублировал
ссылку/уведомления; join-хендлер `chat_member` ставит только `inGroup=true`, не трогает `status`;
обработка выхода/кика из группы (`UserStatus.LEFT`) сознательно вне скоупа фазы.
- 2026-07-05 — **Новый продукт: общая группа (разовый платёж, бессрочный доступ).** Бот заменяет
два существующих бота на одном магазине Робокассы (снимается конфликт Result URL). Вставлена
новая Фаза 4 «Два продукта: общая группа + выбор в /start» ([phase-4.md](phase-4.md)) —
до выдачи доступа, чтобы invite-логика писалась один раз под обе группы; прежние фазы 4–10
перенумерованы в 5–11. prd.md обновлён: п.1/2/3.1/3.6/5.1/6/7/8/9/10/11 — enum `ProductType`
(`SUBSCRIPTION`|`LIFETIME`), `Payment.product`, таблица `CommonAccess`, `Setting.price_common`
(заглушка 500, уточнить), env `COMMON_GROUP_ID`. Cron/напоминания/mute — только закрытая группа.
- 2026-07-05 — Завершена Фаза 4 ([phase-4.md](phase-4.md)): таски 4.1–4.3 ✅ (ProductType/CommonAccess,
`/start` выбор группы, webhook ветвление по продукту + тесты); PR `phase-4` → `dev` — ожидает
подтверждения. Фаза 5 «Выдача доступа в группы» разблокирована.
- 2026-07-05 — Начата Фаза 5 ([phase-5.md](phase-5.md)), 5 тасков (5.1–5.5). Решения: CLAUDE.md
п.7 дополняется исключением — повторная invite-ссылка в общую группу выдаётся без новой оплаты,
только если `CommonAccess.inGroup=false` (юзер вышел); для закрытой группы исключений нет —
только через новую оплату. `chat_member`-хендлер трекает join И left/kicked для обеих групп
(только `inGroup`, `User.status` не трогает). `bot.start()` должен явно запрашивать
`allowed_updates` с `chat_member`. Webhook получает флаг `freshlyProcessed` — повторный
идемпотентный POST не дублирует ссылку/уведомления. Invite-ссылки не хранятся и не отзываются
(риск принят для v1). Task 5.1 (`services/notify.ts`, черновик из `phase-5-task-notify.md`)
взят в работу — перенесён в `TASK.md`, черновой файл удалён.
- 2026-07-06 — Начата Фаза 6 ([phase-6.md](phase-6.md)), 4 таска (6.1–6.4). Миграция не нужна
(`UserStatus.MUTED`/`mutedAt` есть с Фазы 1). Решения: `/admin` в этой фазе получает все три
функции PRD §4 (ручная проверка, сводка, ссылка на панель) — `phases.md` закреплял за Фазой 6
только ручную проверку, сводка и ссылка на панель не были назначены ни одной фазе; unmute при
оплате читает `bot.api.getChat(GROUP_ID).permissions` и передаёт целиком в `restrictChatMember`
(непереданные поля `ChatPermissions` трактуются Telegram как `false`, хардкод урезал бы права);
планировщик — тик `node-cron` раз в минуту (`Europe/Moscow`) со сверкой живого
`Setting.cron_time` и guard «раз в календарный день» (фиксированное cron-выражение потребовало
бы рестарта бота при смене `cron_time` из панели, против правила «не кэшировать»);
`runDailyCheck()` под простым in-process lock — конкурентный вызов (cron/ручная кнопка)
пропускается с логом.
