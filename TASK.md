# TASK: Тесты + ручная проверка матрицы дат

## Фаза

Phase 7 — Напоминания (оба типа, только закрытая группа)

## Статус

🔄 В работе

## Цель

Закрыть Фазу 7: подтвердить, что автотесты Task 7.1–7.3 (`notify.ts`, `subscription.ts`,
`dailyCheck.ts`) без пробелов покрывают весь новый функционал (граничные даты обоих типов
напоминаний, `CommonAccess`-only, конкурентный прогон, best-effort при сбое отправки), и провести
ручную проверку матрицы дат на живом боте — единственное, что автотестами не закрыть.

## Что нужно создать/изменить

- `apps/bot/test/notify.test.ts`, `apps/bot/test/subscription.test.ts`,
  `apps/bot/test/dailyCheck.test.ts` (**bot**) — точечно добавить тесты, только если при финальном
  ревью найдутся реальные пробелы (первый проход пробелов не выявил: граничные даты 4д/3д/повтор и
  9д/10д/20д, `CommonAccess`-only, конкурентный GET_LOCK-прогон, конкурентная оплата между SELECT и
  per-id UPDATE, best-effort при сбое `restrictChatMember`/`sendMessage`/генерации ссылки — уже
  покрыты)
- `.docs/phases/phase-7.md` — по завершении отметить Task 7.4 ✅, фазу ✅
- `.docs/phases/_status.md` — обновить статус Фазы 7 после завершения

## Out of scope

- e2e с реальной Робокассой (уже закрыто ручной проверкой Фазы 3)
- Изменения бизнес-логики `dailyCheck.ts` / `subscription.ts` / `notify.ts` — таск только про
  тесты/ревью/ручную проверку
- PR `phase-7` → `dev` — создаётся отдельно после подтверждения

## ⚠️ Точки риска при реализации

- Ручную матрицу дат (`mutedAt`/`expiresAt`/`reminderSentAt`/`lastMutedRemindAt`) двигать через
  Prisma Studio только на тестовом аккаунте — не на реальном подписчике, иначе можно испортить
  чужие данные.
- `Setting.remind_days`/`muted_remind_days` во время ручной проверки не менять глобально в проде
  (бот и панель делят одну БД) — иначе временно повлияет на реальных пользователей; вернуть
  значения после проверки.

## Definition of Done

- [x] `npm test`, `npm run type-check`, `npm run lint` — чисто
- [x] Ревью тестов на пробелы (граничные даты обоих типов, `CommonAccess`-only, конкурентный
      прогон, best-effort при сбое отправки) — пробелов не найдено
- [ ] Ручная проверка (активные): `expiresAt = now + 4д` → напоминания нет; `= now + 3д` → пришло;
      повторный прогон в тот же период → не пришло второй раз
- [ ] Ручная проверка (замьюченные): `mutedAt = now - 9д` → нет; `= now - 10д` → пришло; сдвиг ещё
      на −10д → снова пришло
- [ ] Ручная проверка: пользователь только с `CommonAccess` не получает напоминаний; после оплаты
      флаги сброшены и цикл начинается заново
- [x] TypeScript компилируется без ошибок
- [x] Проверить `.docs/prompts/dod-global.md` (Cron/конкурентность): граничные даты выборок
      покрыты тестом; флаги напоминаний исключают повторную отправку и сбрасываются при оплате
